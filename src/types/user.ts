export interface NotificationSetting {
    id: number;
    title: string;
    description: string;
    enabled: boolean;
}

export interface SecuritySetting {
    title: string;
    status: string;
    action: string;
}